# Download package AssotesteR from CRAN archive
# url <- "https://cran.r-project.org/src/contrib/Archive/AssotesteR/AssotesteR_0.1-10.tar.gz"
# pkgFile <- "AssotesteR_0.1-10.tar.gz"
# download.file(url = url, destfile = pkgFile)
# Expand the zip file using whatever system functions are preferred

# look at the DESCRIPTION file in the expanded package directory

# Install dependencies list in the DESCRIPTION file

# install.packages(c("ada", "ipred", "evd"))

# Install package
# install.packages(pkgs=pkgFile, type="source", repos=NULL)

# library("AssotesteR")
